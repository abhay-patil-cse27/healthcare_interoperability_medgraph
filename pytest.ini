[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: marks tests as integration tests (require Docker services running)
    unit: marks tests as unit tests (no external dependencies)
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
